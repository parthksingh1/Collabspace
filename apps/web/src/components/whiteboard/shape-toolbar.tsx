'use client';

// Deprecated — the new implementation is in `toolbar.tsx`.
// Re-exported here for backwards compatibility.
import { Toolbar } from './toolbar';
export { Toolbar };
export default Toolbar;
